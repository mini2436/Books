# Legacy Web Status

`web/` is now a deprecated legacy client.

Current policy:

- No new feature development in this directory
- Keep the code as migration reference only
- Future browser and desktop delivery should move to the Flutter codebase in `mobile/`

See:

- `../docs/Web端停用与Flutter多端迁移计划.md`
- `../docs/Flutter应用架构文档.md`
